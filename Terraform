🔹 1. Basics (Every DevOps Engineer Should Know)
What is Terraform?

IaC tool (Infrastructure as Code) → declarative, idempotent, provider-agnostic.

Core Concepts

Providers → AWS, Azure, GCP, Kubernetes, etc.

Resources → actual infra components (EC2, S3, VPC, EKS).

State File (terraform.tfstate) → stores real infra mapping.

Terraform Block (terraform {}) → defines version & backend.

Variables → input values for reusability.

Outputs → return values (public_ip, DNS).

Locals → computed values.

Resource Lifecycle

create_before_destroy → avoids downtime.

prevent_destroy → protects critical resources.

Backends

Local → default (on disk).

Remote → S3, GCS, Azure Blob, Terraform Cloud.

Basic Commands

terraform init → initialize backend & providers.

terraform plan → preview changes.

terraform apply → create infra.

terraform destroy → delete infra.

terraform validate → validate code.

terraform fmt → format code.

✅ Foundation every DevOps engineer must know.

🔹 2. Medium-Level Concepts
Terraform Registry

Official + Community modules (AWS VPC, EKS, etc.).

Reuse existing modules instead of writing from scratch.

Terraform CLI Useful Commands

terraform show → show resources.

terraform state list → list resources in state.

terraform state rm → remove from state.

terraform taint → force recreate a resource.

terraform graph → visualize dependency graph.

Terraform Modules

Reusable code blocks (Best practice for production).

Structure → main.tf, variables.tf, outputs.tf.

Remote State

Store state in S3, lock with DynamoDB.

Avoids local conflicts in teams.

File Organization

Split configs → provider.tf, backend.tf, variables.tf, outputs.tf.

Environment Variables

TF_VAR_name → auto-loads into variables.

AWS_PROFILE, GOOGLE_APPLICATION_CREDENTIALS → provider auth.

Lock Files

.terraform.lock.hcl → locks provider versions.

CLI Config

~/.terraformrc or CLI_CONFIG_FILE → creds helper, registry mirror.

✅ Medium level prepares you for team projects & CI/CD integration.

🔹 3. Advanced Concepts (DevOps Specific)
Terraform Functions

file(), lookup(), join(), merge(), base64encode().

Conditional Expressions
count = var.enabled ? 1 : 0

Count vs For-Each

count → index-based multiple resources.

for_each → map/set-based infra creation.

Dynamic Blocks

Used in complex resources (security groups, IAM policies).

Data Sources

Fetch existing infra (ex: data "aws_ami").

Import

terraform import aws_instance.myec2 i-12345678.

Null Resource + Provisioners

Run scripts via local-exec / remote-exec.

Workspaces

Manage environments (dev, staging, prod).

Lifecycle Meta-Arguments

ignore_changes → don’t overwrite manual changes.

replace_triggered_by → force recreation if dependency changes.

Backend Config Overrides

terraform init -backend-config=backend.hcl.

Plan File

terraform plan -out=tfplan → terraform apply tfplan.

Performance

Use depends_on for ordering.

Split state files (per project/module).

Testing

terraform validate, terraform plan -detailed-exitcode.

Terratest (Go) for automated testing.

Drift Detection

Compare real infra vs Terraform state.

Custom Providers

Written in Go for unsupported services.

Terraform Cloud / Enterprise

Remote runs, teams & RBAC.

Sentinel → policy-as-code.

✅ Advanced topics = FAANG panel favorites.

🔹 4. Real-World DevOps Scenarios
Terraform with AWS

Provision EC2, VPC, EKS, S3, ALB, ASG.

Store state in S3 + DynamoDB locking.

Terraform + Kubernetes (EKS)

Deploy EKS cluster, manage via kubernetes provider.

Terraform CI/CD

Jenkins/GitHub Actions → fmt → validate → plan → apply.

Secrets Management

Use SSM Parameter Store / AWS Secrets Manager / Vault.

Avoid storing secrets in tfstate.

Failure Recovery

If apply fails → rerun. Terraform reconciles state.

Version Management

Lock Terraform version → required_version.

Blue-Green Infra

Manage environments via workspaces or modules.

Hybrid Cloud

One config for AWS + Azure + GCP.

GitOps with Terraform

Use Atlantis/Spacelift → PR-based infra changes.

State File Security

S3 encryption + IAM restricted access.

Multi-Region Deployments

Provider aliasing →

provider "aws" {
  alias  = "use1"
  region = "us-east-1"
}

Cross-Account Infra

Assume roles via STS + provider alias.

Cost Optimization

Use Spot instances.

Lifecycle rules → auto-cleanup unused infra.

✅ Real-world scenarios make you production-ready.
